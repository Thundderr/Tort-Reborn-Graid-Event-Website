# --- Node / Next.js ---
node_modules/
.next/
.next/cache/
out/
.turbo/
.vercel/

# --- Env secrets (do NOT commit) ---
.env
.env.local
.env.*.local

# --- Package manager lockfiles ---
package-lock.json
pnpm-lock.yaml
nul

# --- VS code settings ---
.vscode/

# --- Build artifacts ---
tsconfig.tsbuildinfo

# --- Playwright ---
test-results/
playwright-report/
.playwright-mcp/

# --- Planning notes and specs (kept locally, not in the repo) ---
/docs/

# --- Stray build output ---
/dist/

# --- Claude Code local settings ---
.claude/settings.local.json
# Subagent worktrees — separate checkouts, never part of this one
.claude/worktrees/
